import { Meteor } from 'meteor/meteor';
import '../imports/api/methods';

Meteor.startup(() => {
  // code to run on server at startup
  //Aquí pudieron haber incluido un tag para que informe el idioma en el que la página se encuentra.
});
